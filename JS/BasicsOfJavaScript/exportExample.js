// exportExample.js

// Exporting a variable
export const message = "Hello from exportExample.js";

// Exporting a function
export function capitalizeString(str) {
        return str.toUpperCase();
}

// Exporting a class
export class Calculator {
        add(a, b) {
                return a + b;
        }
}
